const elemenJudul = document.getElementById("judul");
const tombolUbahTeks = document.getElementById("tombolUbahTeks");
const tombolUbahWarna = document.getElementById("tombolUbahWarna");
const tombolReset = document.getElementById("tombolReset");

// TODO: Lengkapi event listener untuk mengubah teks judul saat tombol diklik
// Jika teksnya "Selamat Datang", ubah menjadi "Teks Sudah Diubah!"
// Jika teksnya "Teks Sudah Diubah!", ubah kembali menjadi "Selamat Datang"
// Hint: gunakan if/else dan elemenJudul.innerHTML
tombolUbahTeks.addEventListener("click", function() {
    
});

// TODO: Lengkapi event listener untuk mengubah warna judul saat tombol diklik
// Jika warnanya hitam (atau kosong), ubah menjadi "red"
// Jika warnanya "red", ubah menjadi "blue"
// Jika warnanya "blue", ubah kembali menjadi "black"
// Hint: gunakan if/else dan elemenJudul.style.color
tombolUbahWarna.addEventListener("click", function() {
    
});

// TODO: Lengkapi event listener untuk mengembalikan teks dan warna ke awal
// Hint: kembalikan teks ke "Selamat Datang" dan warna ke "black"
tombolReset.addEventListener("click", function() {
    
});
